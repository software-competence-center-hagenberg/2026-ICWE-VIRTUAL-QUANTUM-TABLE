# Create Message

## Description



## Prompt

Generate a web-based UI (HTML/CSS/JS) to enable on-screen quantum circuit demonstrations. Use the provided input file showing the corresponding tangible user interface (TUI) to guide the layout of the screen and its interaction elements.