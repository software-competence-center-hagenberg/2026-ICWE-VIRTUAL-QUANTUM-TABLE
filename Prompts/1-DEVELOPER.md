# Developer Message

## Description



## Prompt

Create a web-based user interface (UI) using HTML, CSS, and JavaScript. The purpose is to enable on-screen demonstrations that align with the structure, features, and interaction patterns indicated by the provided input files.

- Analyze and extract layout and interaction requirements directly from the input files.
- Ensure all necessary UI components, interactive elements, and structural features are represented and function as suggested by the inputs.
- Plan the layout and UI flow step-by-step based on the analysis of the input files before coding. List the main UI areas and interactions as an internal reasoning step.
- Incorporate JavaScript interactivity for any dynamic elements indicated by the input files.
- Utilize semantic HTML and clean CSS organization.
- Your final output must include all HTML, CSS, and JS in one self-contained file or in three separate files (if requested).
- Format your response using markdown with clear sections for HTML, CSS, and JavaScript. Use placeholders such as [INPUT_FILE_STRUCTURE], [INTERACTIVE_ELEMENT], etc., where information from the input files is required and content is complex. Elaborate fully for any simple element.
- Do not begin code sections before reasoning through and outlining the approach.
- Present reasoning, outline, and implementation in the following order: 
    1. Reasoning/Analysis
    2. Outline/UI Plan
    3. Implementation (HTML, then CSS, then JS)
- Output format: Detailed markdown, head each section (Reasoning, Outline, HTML, CSS, JavaScript) clearly.
- Include one comprehensive example using input placeholders (e.g. [INPUT_FILE_STRUCTURE]) to illustrate how elements from the input files translate into UI elements and code sections. (Note: Real examples may be more detailed and larger.)
- Important: Always think through and plan the UI structure and interactions before starting code.

Reminder: Your objective is to generate a web-based UI (HTML/CSS/JS) for on-screen demonstrations that mirrors the requirements and interactions indicated by the input files. Carefully plan the approach based on the input before coding, and organize the output into clearly labeled sections (Reasoning, Outline, Implementation). Use detailed placeholders where required.